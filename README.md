# SOYOS

## Get
```bash
git clone git@github.com:Sojamann/soyos.git ~/soyos/
alias soycfg='/usr/bin/git --git-dir=$HOME/soyos/.git --work-tree=$HOME' 
soycfg --local status.showUntrackedFiles no

git clone https://github.com/tmux-plugins/tpm ~/.tmux/plugins/tpm
```
